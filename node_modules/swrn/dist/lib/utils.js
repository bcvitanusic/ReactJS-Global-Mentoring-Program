'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.printAndExit = printAndExit;

//判断是否在服务器上运行
var isInServer = exports.isInServer = function isInServer() {
    return typeof SWRN_InServer != 'undefined' && SWRN_InServer;
};

//判断是否处理路由
var isHandleRouter = exports.isHandleRouter = function isHandleRouter() {
    return typeof SWRN_ROUTE != 'undefined' && SWRN_ROUTE;
};

// 打印
function printAndExit(message) {
    var code = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;

    if (code === 0) {
        console.log(message);
    } else {
        console.error(message);
    }

    process.exit(code);
}